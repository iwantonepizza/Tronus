# Report for T-151: backend timezone Asia/Yekaterinburg

**Agent:** Codex  
**Date:** 2026-05-01  
**Status:** `completed`

---

## Summary

Backend display timezone переведён с `UTC` на `Asia/Yekaterinburg`, при этом `USE_TZ = True` сохранён, так что хранение в БД остаётся в UTC. Проверил settings и Django timezone runtime через Docker.

---

## Changes

- `backend/config/settings/base.py` — `TIME_ZONE` изменён на `Asia/Yekaterinburg`.

---

## Tests

- `docker compose run --rm web python manage.py check`
- `docker compose run --rm web python manage.py shell -c "from django.conf import settings; from django.utils import timezone; print(settings.TIME_ZONE); print(timezone.get_default_timezone_name())"`

Результат:
- `manage.py check` — ✓
- runtime timezone / default timezone — `Asia/Yekaterinburg` / `Asia/Yekaterinburg`

---

## Decisions made

- Не трогал `USE_TZ`: задача требовала только display timezone, а не переход на naive datetime.

---

## Deviations from task

- Браузерный smoke Django admin не делал; вместо этого подтвердил timezone через Django runtime в контейнере. Для штатного admin display этого достаточно, потому что Django берёт default timezone из settings.

---

## Open questions / blockers

—

---

## How to verify

1. `cd backend`
2. `docker compose run --rm web python manage.py check`
3. `docker compose run --rm web python manage.py shell`
4. В shell:
   - `from django.conf import settings`
   - `from django.utils import timezone`
   - `settings.TIME_ZONE`
   - `timezone.get_default_timezone_name()`
5. Оба значения должны быть `Asia/Yekaterinburg`.

---

## Checklist (self-review)

- [x] Настройка timezone изменена только на display-уровне.
- [x] `USE_TZ = True` сохранён.
- [x] `manage.py check` проходит.
- [x] Изменение локальное и не меняет API-контракты.
