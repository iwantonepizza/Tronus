# Report for T-080: unify avatar style enum naming

**Agent:** codex
**Date:** 2026-04-23
**Status:** `completed`

---

## Summary

Aligned the avatar style documentation with the actual backend source of truth: `AvatarAsset.style` stays `basic_frame`, and the stale `basic` wording was removed from the data model docs. I also updated the old `T-050` note so the report history no longer points readers at a mismatch that has already been resolved.

---

## Changes

- `ai-docs/DATA_MODEL.md` - updated the `AvatarAsset.style` enum description from `basic` to `basic_frame` and aligned the `source_photo` note with the real MVP value.
- `ai-docs/reports/2026-04-23-T-050.md` - replaced the temporary mismatch note with a resolved-reference note pointing to `T-080`.

---

## Tests

- Exact legacy-value search:
  - `Get-ChildItem backend/apps/avatars, ai-docs -Recurse -File | Select-String -Pattern '\"basic\"'`
- Targeted backend tests:
  - `cd backend && docker compose run --rm web python -m pytest apps/avatars/tests -q -p no:asyncio`
- Targeted lint:
  - `cd backend && ruff check apps/avatars`

Results:
- Avatar tests: `13 passed`
- Ruff: clean
- Exact `"basic"` search: only the self-referential acceptance line in `ai-docs/tasks/BACKLOG.md` remains; no live code or domain docs use `"basic"` as the avatar style enum value.

---

## Decisions made

- Kept the code as the source of truth and standardized docs around `basic_frame`, because that value is already shipped in the model, migration, services, tests, and API responses.
- Did not touch reviewer-owned review logs or task definitions beyond reporting the remaining self-reference, because they are historical process artifacts rather than runtime/domain sources of truth.

---

## Deviations from task

- Touched `ai-docs/reports/2026-04-23-T-050.md` in addition to `DATA_MODEL.md`. This was necessary to avoid leaving a stale historical note that still told readers the mismatch was unresolved.

---

## Open questions / blockers

- None.

---

## How to verify

1. Open `backend/apps/avatars/models.py` and confirm `AvatarAsset.Style.BASIC_FRAME = "basic_frame"`.
2. Open `ai-docs/DATA_MODEL.md` and confirm the `AvatarAsset.style` row now uses `basic_frame`.
3. Run:
   - `cd backend && docker compose run --rm web python -m pytest apps/avatars/tests -q -p no:asyncio`
   - `cd backend && ruff check apps/avatars`
4. Run the exact legacy search:
   - `Get-ChildItem backend/apps/avatars, ai-docs -Recurse -File | Select-String -Pattern '\"basic\"'`
5. Confirm the only remaining hit is the self-referential acceptance line in `ai-docs/tasks/BACKLOG.md`.

---

## Checklist (self-review)

- [x] Docs aligned with backend source of truth.
- [x] No schema migration needed.
- [x] Avatar backend tests pass.
- [x] Ruff is clean for `apps/avatars`.
- [x] Remaining exact `"basic"` hit is documented and understood.
