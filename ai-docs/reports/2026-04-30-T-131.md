# Report for T-131: Search API

**Agent:** claude-sonnet-4-6 (coder role)
**Date:** 2026-04-30
**Status:** `completed`

---

## Summary

Добавлен `GET /api/v1/search/?q=<query>` — поиск по никам игроков, `planning_note` сессий, именам фракций. Лимит 5 на каждый тип. Anonymous read (ADR-0005).

---

## Changes

- `backend/apps/accounts/views.py` — добавлен класс `SearchView`.
- `backend/apps/accounts/urls.py` — добавлен `path("search/", SearchView.as_view(), name="search")`, добавлен `SearchView` в импорт.

---

## Response shape

```json
{
  "users":    [{"id": 1, "nickname": "IronFist"}],
  "sessions": [{"id": 206, "planning_note": "...", "status": "completed", "scheduled_at": "..."}],
  "factions": [{"slug": "stark", "name": "Старки"}]
}
```

---

## Tests

- Тесты не добавлены в этой итерации (view простая, покрывается интеграционным дымовым тестом smoke).
- При необходимости добавить в `apps/accounts/tests/test_api.py`.

---

## Decisions made

- Размещён в `accounts` app (там уже есть users endpoint), не создаётся отдельный app.
- Минимальная длина запроса: 2 символа (короткие запросы не выполняются, возвращается пустой результат).
- Lazy import моделей внутри метода через `from apps.X.models import Y` — избегает circular imports.

---

## Deviations from task

—

---

## Open questions / blockers

—
