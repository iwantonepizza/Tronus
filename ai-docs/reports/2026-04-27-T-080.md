# Report for T-080: Unify avatar style enum naming

**Agent:** claude-sonnet-4-6 (coder role)
**Date:** 2026-04-27
**Status:** `completed`

---

## Summary

Код уже содержит правильное значение `basic_frame` повсюду. Расхождение было только в документации. Исправлено два упоминания `basic` в `ai-docs/DATA_MODEL.md`, ADR-0007 уже был актуален.

---

## Changes

- `ai-docs/DATA_MODEL.md` — строки `style` и `source_photo` в таблице `avatars`: `basic` → `basic_frame`.

---

## Tests

- Новых тестов нет (задача документационная).
- Acceptance criteria: `grep -r '"basic"' backend/apps/avatars/ ai-docs/` — возвращает только строку из BACKLOG (описание AC самой задачи), не enum-значений.

---

## Decisions made

Источник истины — код, как и указал architect в задаче. ADR-0007 уже содержал `basic_frame`, правок не потребовал.

---

## Deviations from task

—

---

## Open questions / blockers

—

---

## How to verify

```bash
grep -rn '"basic"' backend/apps/avatars/ ai-docs/DATA_MODEL.md
# Ожидаемый результат: пусто
```

---

## Checklist (self-review)

- [x] Тесты написаны и проходят (не требуются — doc-only).
- [x] `ruff check` чистый (Python не затронут).
- [x] Миграции не менялись.
- [x] Нет ORM во views (не затронуто).
- [x] Нет бизнес-логики в models/serializers (не затронуто).
- [x] Публичный API не менялся.
- [x] Отклонений нет.
