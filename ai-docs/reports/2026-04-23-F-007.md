# Report for F-007: Player profile, leaderboard, factions and head-to-head stats screens

**Agent:** codex
**Date:** 2026-04-23
**Status:** `completed`

---

## Summary

Closed the remaining frontend stats gap by implementing the dedicated `/h2h` comparison screen on top of the real backend head-to-head endpoint. The route is no longer a placeholder and now uses typed API/domain mapping plus query-driven player selection.

---

## Changes

- `frontend/src/api/types.ts` — added API contracts for `head-to-head`.
- `frontend/src/types/domain.ts` — added domain models for comparison payloads.
- `frontend/src/api/stats.ts` — added `getHeadToHeadStats(...)`.
- `frontend/src/api/mappers.ts` — mapped backend comparison payload to frontend domain shape.
- `frontend/src/hooks/useStats.ts` — added `useHeadToHeadStats(...)`.
- `frontend/src/pages/HeadToHeadPage.tsx` — implemented comparison UI with player selectors, rivalry summary and shared matches list.
- `frontend/src/App.tsx` — replaced `/h2h` placeholder route with the real page.
- `frontend/tests/head-to-head-page.test.tsx` — added dedicated page tests.
- `frontend/tests/stats-pages.test.tsx` — added h2h rendering coverage.
- `frontend/tests/app.test.tsx` — extended `useStats` mock to include the new export.

---

## Tests

- Added frontend coverage for the `/h2h` route.
- Commands:
  - `npm run lint`
  - `npm run test`
  - `npm run build`

---

## Decisions made

- Bound player selection to query params `user_a` / `user_b`, so the comparison page is shareable and survives refresh without adding extra local state.
- Kept the page on the real stats API path instead of mock fallback, because the backend endpoint `T-044` is already available and this closes the remaining placeholder in the stats navigation.

---

## Deviations from task

- `F-007` in backlog is phrased as mock-based stats screens, but those screens were already implemented earlier and later switched to real API in `F-009`. This report therefore captures the remaining missing stats-screen piece: the dedicated `/h2h` route.

---

## Open questions / blockers

- —

---

## How to verify

1. `cd frontend`
2. `npm install`
3. `npm run lint && npm run test && npm run build`
4. Open `/h2h`, choose two players, confirm the page shows wins, higher placements and shared matches.

---

## Checklist (self-review)

- [x] Tests written and passing.
- [x] Frontend lint clean.
- [x] Public route `/h2h` no longer uses a placeholder.
- [x] New UI uses typed API/domain mapping.
- [x] Deviations from backlog wording described above.
