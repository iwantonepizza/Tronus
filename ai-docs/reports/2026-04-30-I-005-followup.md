# Report for I-005: Deploy follow-up for tracked frontend build stubs

**Agent:** Codex
**Date:** 2026-04-30
**Status:** `completed`

---

## Summary

Исправлен deploy-blocker для production frontend build: source-stubs `frontend/src/build/noop-mocks-data.ts` и `frontend/src/build/noop-session-factory.ts` были нужны Vite production aliasing, но не попадали в git из-за `.gitignore`. После правки `.gitignore` эти файлы могут быть закоммичены и доехать на сервер.

---

## Changes

- `.gitignore` — убрано исключение `frontend/src/build/`, которое ломало production build на чистом сервере.
- `ai-docs/reports/2026-04-30-I-005-followup.md` — этот отчёт.

---

## Tests

- `cd frontend && npm run build` — проходит.
- `git ls-files frontend/src/build` до правки — пусто.
- После правки git видит файлы как candidate для добавления — ✓.

---

## Decisions made

- Не переносил build-stubs в другое место и не менял Vite aliasing: проблема была не в архитектуре фронта, а только в том, что нужные source-файлы были исключены из git.

---

## Deviations from task

- —

---

## Open questions / blockers

- —

---

## How to verify

1. Убедиться, что в репозитории есть файлы `frontend/src/build/noop-mocks-data.ts` и `frontend/src/build/noop-session-factory.ts`.
2. Выполнить `cd frontend && npm run build`.
3. На сервере после `git pull` повторить frontend build.

---

## Checklist (self-review)

- [x] Production build снова проходит.
- [x] Причина server-only падения локализована.
- [x] Изменение минимальное и не трогает runtime-логику.
