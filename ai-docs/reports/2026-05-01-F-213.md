# Report for F-213: cancelled UI для карточки партии

**Agent:** Codex  
**Date:** 2026-05-01  
**Status:** `completed`

---

## Summary

Довёл `MatchDetailPage` для отменённой партии до read-only сценария. Для `cancelled` теперь не показывается пустой блок про будущую турнирную таблицу; на экране остаются planning info, read-only invite roster и чат.

---

## Changes

- `frontend/src/pages/MatchDetailPage.tsx` — добавлена отдельная ветка рендера для `match.status === 'cancelled'`: вместо placeholder про турнирную таблицу сразу показывается `CommentThread`.

---

## Tests

- `cd frontend && npm run lint`
- `cd frontend && npm run test`
- `cd frontend && npm run build`

Результат:
- lint — ✓
- full frontend suite — ✓ (`51 passed`)
- production build — ✓

Примечание:
- Существующий regression в `frontend/tests/matches-pages.test.tsx` продолжает проходить без отдельного расширения тестового набора.

---

## Decisions made

- Не добавлял новый cancelled-only декоративный блок: текущая задача про снятие блокирующего мусорного UI, а не про новый визуальный сценарий. Минимальный безопасный вариант — оставить комментарии и убрать пустую турнирную секцию.

---

## Deviations from task

- —

---

## Open questions / blockers

- —

---

## How to verify

1. Открыть cancelled match `/matches/<id>`.
2. Убедиться, что видны planning note, read-only invites и чат.
3. Убедиться, что нет блока `Турнирная таблица появится после старта партии`.

---

## Checklist (self-review)

- [x] Cancelled match не показывает пустую турнирную секцию.
- [x] Read-only roster и чат сохранены.
- [x] `npm run lint` проходит.
- [x] `npm run test` проходит.
- [x] `npm run build` проходит.
