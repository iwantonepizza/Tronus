# Report for I-002: end-to-end smoke test in dev

**Agent:** Codex
**Date:** 2026-04-23
**Status:** `completed`

---

## Summary

Ran the full dev smoke path across the real integration contour: `frontend Vite dev server -> proxy /api -> backend Docker/Postgres`. The path `register -> approve via admin -> login -> create session -> add 3 participants -> finalize -> verify in completed list -> logout` passed without code changes.

---

## Changes

- `ai-docs/reports/2026-04-23-I-002.md` - smoke test report and verification notes.

---

## Tests

- Clean dev backend startup:
  - `docker compose down -v`
  - `docker compose up -d db web`
  - `docker compose exec web python manage.py migrate`
- Prepared smoke actors:
  - created admin user in backend runtime
  - created three active player users in backend runtime
- Smoke execution:
  - started `frontend` via `npm run dev -- --host 127.0.0.1`
  - requested `http://127.0.0.1:5173/`
  - called `GET /api/v1/auth/csrf/` through Vite proxy
  - registered a new user through proxy
  - approved that user through Django admin HTTP action `approve_selected_users`
  - logged in through proxy
  - created a session through proxy
  - added three participants through proxy
  - finalized the session through proxy
  - verified the session in `GET /api/v1/sessions/?status=completed`
  - logged out through proxy
- Result: smoke passed end-to-end.

---

## Decisions made

- Used Django admin HTTP action for approval instead of direct DB mutation, because the task explicitly requires approval through admin semantics.
- Ran the smoke against `127.0.0.1:5173/api/...` instead of direct backend URLs, because the integration task is specifically about the frontend-backend dev linkage and proxy path.

---

## Deviations from task

- No browser automation was added. The smoke was executed as HTTP-level end-to-end verification through the live Vite proxy and live backend/admin, which validates the same integration contour but not visual rendering.

---

## Open questions / blockers

- —

---

## How to verify

1. `cd backend`
2. `docker compose down -v`
3. `docker compose up -d db web`
4. `docker compose exec web python manage.py migrate`
5. Ensure an admin user exists.
6. In another shell: `cd frontend && npm run dev -- --host 127.0.0.1`
7. Open `http://127.0.0.1:5173/`
8. Go through:
   - register
   - approve user in `/admin/`
   - login
   - create session
   - add 3 participants
   - finalize
   - verify session appears in matches list

---

## Checklist (self-review)

- [x] Smoke path executed on real dev services.
- [x] Frontend requests went through Vite proxy.
- [x] Approval used admin behavior, not direct DB edit.
- [x] Finalized session appeared in completed list.
- [x] No integration defects were found in this path.
