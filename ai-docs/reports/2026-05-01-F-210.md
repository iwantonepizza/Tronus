# F-210 Report

## Summary

- Reworked `MatchDetailPage` for `planned/cancelled` sessions around invite roster instead of standings.
- Extended invite UI with avatar, desired faction summary, RSVP states, declined collapse, and creator/admin delete affordance.
- Added mock invite support so the Wave 10 UX can be covered in Vitest without backend calls.

## Files touched

- `frontend/src/api/types.ts`
- `frontend/src/mocks/data.ts`
- `frontend/src/hooks/useSessions.ts`
- `frontend/src/components/match/RsvpBlock.tsx`
- `frontend/src/pages/MatchDetailPage.tsx`
- `frontend/tests/matches-pages.test.tsx`

## Tests

- Planned:
  - `npm run lint`
  - `npm run test -- matches-pages.test.tsx`
  - `npm run build`

## Deviations from task

- Cancelled-session behaviour from `F-213` was folded into this rework because it touches the same `MatchDetailPage` state split and would otherwise duplicate the conditional layout logic.
