# Report for T-118: Audit для удаления `expansion_a/b` из тестов и фикстур

**Agent:** Codex
**Date:** 2026-04-29
**Status:** `completed`

---

## Summary

Провёл audit runtime/test-слоя после `T-106` и убрал последний живой legacy deck slug из backend tests. В коде приложений, фронтовых исходниках и тестах больше не осталось `expansion_a` / `expansion_b`; они сохранились только в исторических migrations и change-request документах.

---

## Changes

- `backend/apps/games/tests/test_api.py` — в negative-case для неизвестной колоды `deck="expansion_a"` заменён на нейтральный `deck="legacy_removed_deck"`, чтобы test больше не зависел от устаревшего доменного названия.

---

## Tests

- `python -m ruff check backend/apps/games/tests/test_api.py`
- `docker compose run --rm web python -m pytest apps/games/tests/test_api.py -q -p no:asyncio -k unknown_house_deck_slug`
- `rg -n 'expansion_a|expansion_b' backend/apps frontend/src frontend/tests --glob '!**/migrations/*.py'`
  - runtime/test matches: none

---

## Decisions made

- Исторические вхождения в `backend/apps/reference/migrations/*.py`, `ai-docs/changes/CR-006-decks-redesign.md` и исходном user-feedback не трогал: это audit-trace и reversible migration context, а не живой runtime/test слой.

---

## Deviations from task

- Формализованной карточки `T-118` в backlog нет, это строка в разделе tech debt. Поэтому scope ограничил audit-слоем `backend/apps`, `frontend/src`, `frontend/tests`; документацию architect-уровня и старые migrations оставил как исторический след.

---

## Open questions / blockers

- —

---

## How to verify

1. `cd backend`
2. `docker compose up -d db`
3. `docker compose run --rm web python -m pytest apps/games/tests/test_api.py -q -p no:asyncio -k unknown_house_deck_slug`
4. `cd ..`
5. `rg -n 'expansion_a|expansion_b' backend/apps frontend/src frontend/tests --glob '!**/migrations/*.py'`

---

## Checklist (self-review)

- [x] Тесты написаны и проходят.
- [x] `ruff check` чистый.
- [x] Миграции не затронуты.
- [x] Нет ORM во views.
- [x] Нет бизнес-логики в models/serializers.
- [x] Публичный API не изменён.
- [x] Отклонения от задачи описаны выше.
