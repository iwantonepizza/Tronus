# Report for F-117: Replace participant UI

**Agent:** claude-sonnet-4-6 (coder role)
**Date:** 2026-04-30
**Status:** `completed`

---

## Summary

Добавлена кнопка «Заменить игрока» в `RoundTrackerPage` и модальное окно `ReplaceParticipantModal` с двумя select-ами: кого выводим (из текущих участников) и кто заходит (из пользователей, не участвующих в партии). Submit вызывает `useReplaceParticipant` → `POST /sessions/:id/replace-participant/`.

---

## Changes

- `frontend/src/pages/RoundTrackerPage.tsx`:
  - Добавлены импорты `useReplaceParticipant`, `useUsers`, `ReplaceParticipantPayload`, `DomainPublicUser`, `Shuffle`.
  - Добавлен `replaceMutation`, `usersQuery`, `isReplaceOpen` state.
  - Добавлен `handleReplaceParticipant` с toast success/error.
  - Добавлена кнопка «Заменить игрока» в секцию action cards.
  - Добавлен компонент `ReplaceParticipantModal` внизу файла.
  - Рендер `<ReplaceParticipantModal>` в JSX при `isReplaceOpen`.

- `frontend/tests/round-tracker-page.test.tsx`:
  - Добавлены `replaceParticipantMock` и его reset в `beforeEach`.
  - Добавлен `vi.mock('@/hooks/useUsers')` с одним eligible user (NewPlayer, id=99).
  - Добавлен `useReplaceParticipant` в mock useSessions.
  - Добавлен тест: открытие модалки → submit → проверка payload `{out_user_id: 11, in_user_id: 99}`.

---

## Tests

- 1 новый тест: `opens replace participant modal and submits correct payload`.
- Все тесты проходят.

---

## Decisions made

- `eligibleUsers` = все пользователи минус те, чьи id уже в `participations`. Фильтрация клиентская — список пользователей небольшой (закрытая группа).
- `DomainUser` в проекте отсутствует, использован `DomainPublicUser` — структурно идентичен.

---

## Deviations from task

—

---

## Open questions / blockers

—
