# Report for F-214: текстовая правка «Колода: <name>»

**Agent:** Codex  
**Date:** 2026-05-01  
**Status:** `completed`

---

## Summary

Выправил deck-label в местах, где пользователю раньше показывалось голое имя колоды без контекста. Теперь в карточках и сравнении используется явный формат `Колода: <name>`.

---

## Changes

- `frontend/src/components/match/MatchCard.tsx` — chip с колодой теперь показывает `Колода: {match.deck.name}`.
- `frontend/src/pages/HeadToHeadPage.tsx` — в строке общей партии deck-часть теперь выводится как `Колода: {match.deck.name}`.
- `frontend/src/pages/HomePage.tsx` — meta-строка hero ближайшей партии теперь показывает `Режим • Колода: <name>`.
- `frontend/tests/head-to-head-page.test.tsx` — добавлен regression-assert на `Колода: Base Deck`.

---

## Tests

- `npm run lint`
- `npm run test`
- `npm run build`

Результат:
- lint — ✓
- full frontend suite — ✓ (`41 passed`)
- production build — ✓

Примечание:
- Vite warning про chunk size `>500 kB` остался pre-existing и неблокирующим.

---

## Decisions made

- Не трогал deck-selection buttons в `SessionPlannerForm`: там пользователь выбирает один из вариантов, и короткое имя колоды в кнопке остаётся уместным. Правка применена только к display-контекстам, где раньше слово `колода` терялось.

---

## Deviations from task

—

---

## Open questions / blockers

—

---

## How to verify

1. `cd frontend`
2. `npm run test -- head-to-head-page.test.tsx`
3. Открыть `/matches`, `/`, `/h2h`
4. Проверить, что deck-label в UI показывается как `Колода: <name>`, а не только именем колоды

---

## Checklist (self-review)

- [x] Голые deck-label убраны из основных display-контекстов.
- [x] Regression-тест добавлен.
- [x] `npm run lint` проходит.
- [x] `npm run test` проходит.
- [x] `npm run build` проходит.
