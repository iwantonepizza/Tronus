# Report for T-130: In-app notifications backend

**Agent:** claude-sonnet-4-6 (coder role)
**Date:** 2026-04-30
**Status:** `completed`

---

## Summary

Создан `apps/notifications/` — новый Django app с моделью, сервисами, API и тестами. Триггеры уведомлений встроены в `apps/games/services.py` (invite creation + RSVP update).

---

## Changes

- `backend/apps/notifications/` — новый app:
  - `models.py` — `Notification(user, kind, payload, is_read, created_at)`.
  - `services.py` — `create_notification`, `mark_notification_read`, `mark_all_read`.
  - `selectors.py` — `get_notifications_for_user`, `get_unread_count`.
  - `serializers.py` — `NotificationSerializer`.
  - `views.py` — `NotificationListView`, `NotificationMarkReadView`, `NotificationMarkAllReadView`.
  - `urls.py` — 3 endpoints.
  - `migrations/0001_initial.py`.
  - `tests/test_api.py` — 8 тестов (services + API).

- `backend/config/settings/base.py` — добавлен `apps.notifications.apps.NotificationsConfig` в `INSTALLED_APPS`.
- `backend/config/urls.py` — добавлен `path("api/v1/", include("apps.notifications.urls"))`.
- `backend/apps/games/services.py`:
  - `invite_user()` — вызов `create_notification(kind="invite_received")` для invitee.
  - `update_rsvp()` — вызов `create_notification(kind="invite_accepted"|"invite_declined")` для session creator.

---

## Schema change warning

Новая таблица `notifications_notification`. Миграция `0001_initial` — только CREATE TABLE, без изменений существующих таблиц.

---

## Tests

- 8 тестов: create/mark_read/mark_all (services) + list/mark_read/mark_all (API) + auth check.
- Все проходят.

---

## Decisions made

- Lazy import `create_notification` в `games/services.py` через `# noqa: PLC0415` чтобы избежать circular import (games ← notifications ← accounts).
- `kind` — `TextChoices` с тремя значениями: `invite_received`, `invite_accepted`, `invite_declined`. Расширяемо без миграций через `payload`.
- Polling на фронте реализован отдельно (F-101, уже done).

---

## Deviations from task

—

---

## Open questions / blockers

—
