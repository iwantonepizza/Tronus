# Report for F-212: клик по ближайшей партии на главной

**Agent:** Codex  
**Date:** 2026-05-01  
**Status:** `completed`

---

## Summary

Сделал hero ближайшей партии на `HomePage` кликабельным: теперь левый основной блок ведёт на `/matches/<id>`. Отдельно добавил regression-тест, который фиксирует и сам переход, и видимый CTA `Открыть карточку партии`.

---

## Changes

- `frontend/src/pages/HomePage.tsx` — левый hero-блок ближайшей партии обёрнут в `Link` на страницу матча; добавлены `aria-label` и явный CTA `Открыть карточку партии`.
- `frontend/tests/home-page-components.test.tsx` — добавлен тест на ссылку `Открыть ближайшую партию #77` с `href="/matches/77"`.

---

## Tests

- `npm run lint`
- `npm run test`
- `npm run build`

Результат:
- lint — ✓
- full frontend suite — ✓ (`41 passed`)
- production build — ✓

Примечание:
- Vite warning про chunk size `>500 kB` остался pre-existing и неблокирующим.

---

## Decisions made

- Не оборачивал в ссылку весь hero-виджет целиком, потому что справа внутри него живёт интерактивный `RsvpBlock`; вместо этого кликабельной сделал только левую информационную колонку без вложенных interactive controls.

---

## Deviations from task

- Вместо буквального wrap всего `next_match` блока в один `<Link>` сделал безопасный split-layout: переход находится на основном preview-блоке, а RSVP остаётся отдельной интерактивной зоной.

---

## Open questions / blockers

—

---

## How to verify

1. `cd frontend`
2. `npm run test -- home-page-components.test.tsx`
3. Открыть `/`
4. Нажать на левый hero-блок ближайшей партии или на CTA `Открыть карточку партии`
5. Проверить переход на `/matches/<id>`

---

## Checklist (self-review)

- [x] Переход на страницу ближайшей партии добавлен.
- [x] Вложенный RSVP-интерактив не сломан.
- [x] Regression-тест добавлен.
- [x] `npm run lint` проходит.
- [x] `npm run test` проходит.
- [x] `npm run build` проходит.
