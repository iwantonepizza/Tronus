# Report for T-081: Production WSGI server в draft deploy stack

**Agent:** claude-sonnet-4-6 (coder role)
**Date:** 2026-04-27
**Status:** `completed`

---

## Summary

Добавлен `gunicorn` в зависимости, создан `backend/Dockerfile.prod` с gunicorn CMD, `deploy/docker-compose.prod.yml` переключён на prod-Dockerfile. Dev-стек (`backend/docker-compose.yml`, `backend/Dockerfile`) не тронут.

---

## Changes

- `backend/requirements.in` — добавлена строка `gunicorn>=22.0,<24.0`.
- `backend/requirements.txt` — добавлена pinned строка `gunicorn==22.0.0` (pip-compile нужно перегнать в окружении с pip-tools; пин выставлен вручную как placeholder).
- `backend/Dockerfile.prod` — создан. Идентичен `Dockerfile` по образу и установке зависимостей, отличается только CMD: `gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers 3 --timeout 60 --access-logfile -`.
- `deploy/docker-compose.prod.yml` — `backend.build.dockerfile` изменён на `Dockerfile.prod`, `command` теперь вызывает gunicorn вместо `runserver`.
- `deploy/README.md` — убрана оговорка про runserver, добавлена строка про gunicorn.

---

## Tests

- Новых pytest-тестов нет (infra задача).
- Acceptance criteria «dev runserver нетронут» верифицирован: `backend/Dockerfile` содержит прежний CMD.

---

## Decisions made

- `requirements.txt` обновлён вручную (pip-compile нет в агентском окружении). Нужно пересобрать командой `pip-compile backend/requirements.in -o backend/requirements.txt` перед реальным деплоем. Указал версию 22.0.0 — последняя стабильная на момент задачи.
- `--workers 3` — стандартный рекомендованный старт для малонагруженного сервера (2×CPU+1 при 1 CPU). Можно увеличить через env-переменную если понадобится, но хардкод в CMD приемлем для draft.
- CMD дублируется и в `Dockerfile.prod`, и в `docker-compose.prod.yml`. В Compose `command:` переопределяет Dockerfile CMD — это нормально, Dockerfile CMD служит документацией дефолта.

---

## Deviations from task

- `requirements.txt` обновлён вручную, а не через `pip-compile` (инструмент недоступен в агентском окружении). Отмечено выше.

---

## Open questions / blockers

—

---

## How to verify

```bash
# Проверить что dev не сломан:
cd backend && docker compose up  # runserver на :8000

# Проверить prod-стек:
cd deploy && docker compose -f docker-compose.prod.yml up --build
curl http://localhost:8080/api/v1/reference/factions/  # ожидаем 200

# Если pip-tools доступен — пересобрать pinned requirements:
pip-compile backend/requirements.in -o backend/requirements.txt
```

---

## Checklist (self-review)

- [x] Тесты не требуются (infra).
- [x] `ruff check` не затронут (Python-файлов нет).
- [x] Миграции не менялись.
- [x] `backend/docker-compose.yml` нетронут.
- [x] `backend/Dockerfile` нетронут (dev runserver жив).
- [x] Публичный API не менялся.
- [x] Отклонение по pip-compile описано выше.
