# Report for F-113: Wildlings raid action

**Agent:** Codex
**Date:** 2026-04-29
**Status:** `completed`

---

## Summary

Добавил в `RoundTrackerPage` полноценный 3-шаговый wizard для действия `Wildlings raid`: ставки участников, выбор исхода `win/loss`, выбор placeholder-карты и новой отметки угрозы. Заодно довёл фронт до рабочего состояния после накопившихся рассинхронов: починил build-типизацию, обновил stale frontend tests и вернул общий suite в зелёное состояние.

---

## Changes

- `frontend/src/pages/RoundTrackerPage.tsx` — добавлен action-блок для одичалых, `WildlingsRaidWizard`, submit через `useRecordWildlingsRaid`, success/error toast.
- `frontend/tests/round-tracker-page.test.tsx` — новый тест на 3-шаговый submit wizard-а и точный payload для `recordWildlingsRaid`.
- `frontend/src/components/ui/Button.tsx` — добавлен проп `fullWidth`, который уже фактически использовался в страницах и ломал `tsc` на production build.
- `frontend/src/api/types.ts` — восстановлен пропавший интерфейс `SessionListFilters`; файл больше не ломает сборку по синтаксису.
- `frontend/src/types/domain.ts` — импортированы `RsvpStatus` и `TimelineEventKind`, чтобы `domain.ts` снова собирался без TS ошибок.
- `frontend/src/pages/MatchStartPage.tsx` — убран `setState` внутри `useEffect`; логика assignment переведена на derived state, чтобы ESLint снова был зелёным.
- `frontend/tests/matches-pages.test.tsx` — обновлён stale-тест под текущее RSVP-loading поведение.
- `frontend/tests/session-flow-pages.test.tsx` — обновлены stale assertions под текущий Phase 2 flow создания/финализации.

---

## Tests

- Добавлено тестов: 1 новый UI-тест.
- Команда запуска: `npm run lint`, `npm run test`, `npm run build`.
- Все проходят: ✓.
- Итоговый frontend suite: `11 files`, `31 passed`.

---

## Decisions made

- Не стал в этой задаче разворачивать полноценный timeline UI: `F-113` ограничен именно action-wizard для записи атаки, а визуализация хронологии уже отдельно лежит в `F-116`.
- В шаг 3 wizard-а включил не только выбор placeholder-карты, но и `wildlings_threat_after`, потому что backend API требует это поле уже сейчас.
- Исправил несколько внеплановых frontend build/test блокеров в рамках этой задачи, потому что без них новый экран нельзя было бы считать реально интегрированным.

---

## Deviations from task

- Помимо самого `F-113` пришлось поправить технические хвосты в `Button`, `api/types`, `domain.ts`, `MatchStartPage` и stale tests. Это не изменение продукта по смыслу, а восстановление рабочего frontend-контра после накопившихся рассинхронов.

---

## Open questions / blockers

- —

---

## How to verify

1. `cd frontend`
2. `npm install` при необходимости
3. `npm run lint`
4. `npm run test`
5. `npm run build`
6. В dev-режиме открыть active match → `/matches/:id/rounds`
7. Нажать `Атака одичалых`
8. Пройти 3 шага wizard-а и убедиться, что появляется success toast

---

## Checklist (self-review)

- [x] Тесты написаны и проходят.
- [x] `ruff check` чистый.
- [x] Миграции применяются на чистой БД.
- [x] Нет ORM во views.
- [x] Нет бизнес-логики в models/serializers.
- [x] Публичный API соответствует `API_CONTRACT.md`.
- [x] Отклонения от задачи описаны выше.
