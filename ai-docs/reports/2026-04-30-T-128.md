# Report for T-128: replace placeholder Westeros card slugs

**Agent:** Codex
**Date:** 2026-04-30
**Status:** `completed`

---

## Summary

Заменил placeholder-slugs карт Вестероса на актуальные значения из `ai-docs/WESTEROS_DECKS.md` в backend и во фронтовом round tracker. Структура колод и API не менялись, миграции не потребовались. Для защиты от регресса добавил backend unit-tests и обновил фронтовый test-case выбора карт.

---

## Changes

- `backend/apps/games/event_cards.py` — обновлены `WESTEROS_DECKS` для `classic`, `feast_for_crows`, `dance_with_dragons`, `mother_of_dragons`; убрана пометка про placeholder-slugs в docstring.
- `backend/apps/games/tests/test_event_cards.py` — новые unit-tests на состав classic/FFC/MoD колод и переиспользование общих deck lists.
- `frontend/src/pages/RoundTrackerPage.tsx` — синхронизирован `EVENT_DECKS` с backend; уточнён helper-текст, что placeholder пока остаются только у `wildlings` outcome cards.
- `frontend/tests/round-tracker-page.test.tsx` — обновлён event-card selection test под новый slug `web_of_lies`; выровнен stale expectation в replace-flow (`out_user_id`).

---

## Tests

- `python -m ruff check backend/apps/games/event_cards.py backend/apps/games/tests/test_event_cards.py`
- `docker compose run --rm web python -m pytest apps/games/tests/test_event_cards.py -q -p no:asyncio`
- `npm run test -- round-tracker-page.test.tsx`

Результат:
- backend ruff — зелёный
- backend tests — `2 passed`
- frontend targeted tests — `4 passed`

---

## Decisions made

- Взял `ai-docs/WESTEROS_DECKS.md` как текущий owner source of truth для `T-128`, несмотря на старую пометку `blocked` в backlog.
- Для `feast_for_crows` deck 1 и `mother_of_dragons` deck 4 использовал slugs ровно из `WESTEROS_DECKS.md`, потому что файл был передан владельцем как актуальный референс в чате.

---

## Deviations from task

- `frontend/tests/round-tracker-page.test.tsx` пришлось поправить не только под новые card slugs, но и под фактический payload replace-flow (`out_user_id`), потому что старое ожидание смотрело на `participation.id`, а сервис работает с `user.id`.

---

## Open questions / blockers

- `WILDLINGS_OUTCOME_CARDS` не менял: в `ai-docs/tasks/BACKLOG.md` и `ai-docs/source/USER_FEEDBACK_ANALYSIS_2026-04-27.md` они всё ещё отмечены как отдельная заглушка до следующего owner list.
- Полный frontend `lint/build` сейчас красный по соседним файлам, не связанным с `T-128`:
  - `frontend/src/components/ui/SearchPalette.tsx` — `react-hooks/set-state-in-effect`
  - `frontend/src/pages/FinalizeSessionPage.tsx` — синтаксическая ошибка TypeScript
  Это уже логично брать следующим шагом под `T-129`.

---

## How to verify

1. `cd backend`
2. `docker compose run --rm web python -m pytest apps/games/tests/test_event_cards.py -q -p no:asyncio`
3. `cd ../frontend`
4. `npm run test -- round-tracker-page.test.tsx`
5. Открыть `/matches/:id/rounds` и проверить, что в wizard `Карты Вестероса` отображаются новые slugs из `WESTEROS_DECKS.md`.

---

## Checklist (self-review)

- [x] Тесты написаны и проходят.
- [x] `ruff check` чистый.
- [x] Миграции не требовались.
- [x] Нет ORM во views.
- [x] Нет бизнес-логики в models/serializers.
- [x] Публичный API не менялся.
- [x] Отклонения от задачи описаны выше.
