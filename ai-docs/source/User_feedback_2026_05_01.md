# User feedback — 2026-05-01 (Wave 11 trigger)

Исходный текст владельца, без изменений. Анализ — в `USER_FEEDBACK_ANALYSIS_2026-05-01.md`.

---

тех кого изначально пригласили в партию при создании, попадают в нее при создании как под вопросом, а то так сейчас собираешь пати, добавляешь, а создается пустая.

Кнопка под вопросом не нажимается пока не нажмешь я иду, только потом как будто разблокируется, нужно чтобы сразу можно было  и ты вступаешь под вопросом

И все еще есть путаница, в меню редактирования партии есть меню пригласить игроков, туда так же если пригласить кого то он должен отобразится в партии как под вопросом.

Начать партия по обычному тоже ен получается.

GET /api/v1/sessions/10/start/
HTTP 500 Internal Server Error
{
    "error": {
        "code": "server_error",
        "details": {
            "exception_class": "MethodNotAllowed",
            "exception_message": "Метод \"GET\" не разрешен."
        }
    }
}

[backend log: POST /api/v1/sessions/10/start/ HTTP/1.1" 400 192]
[backend log: Unhandled exception in games view: Метод "GET" не разрешен.]

При вводе пароля ( логин, регистрация, восстановление пароля) чтобы пароль при нажатии на кнопочку показывался.

Очень важно, при восстановлении пароля у незалогиненого пользователя убрать плейсхолджер секретного слова, это бред его там показывать + восстановление только по почте, но не по нику, по нику легко взломать, секретное слово то все знаюют

---

[Дополнительно — проблема с быстрой отметкой:]

не могу создать партию ( быстрая отметка )
пишет — Ошибка валидации.
POST https://got.craft-hookah.ru/api/v1/sessions/8/finalize-played/ 400 (Bad Request)
[backend log: 400 182 — детали в response body не показаны юзером]